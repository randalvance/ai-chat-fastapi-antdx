[project]
name = "ai-chat-fastapi-antdx"
version = "0.1.0"
description = "FastAPI AI Chat Application with AntD"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.128.0",
    "uvicorn[standard]>=0.40.0",
]
